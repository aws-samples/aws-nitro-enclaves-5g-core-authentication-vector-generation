---
schemaVersion: '1.0'
description: Document to hold regional settings for Session Manager
sessionType: Standard_Stream
inputs:
  runAsEnabled: true
  runAsDefaultUser: 'ec2-user'
  idleSessionTimeout: '20'
  maxSessionDuration: '120'
  shellProfile:
    linux: 'cd $HOME && bash -l'
